package covidtest;

/**
 * An interface that dictates the required methods for the different formats of tests that might be conducted
 * with the interview.
 */
public interface TestMethods {
    void questionForm();
    int getResults();
}
